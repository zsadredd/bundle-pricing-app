# Bundle Pricing App (Private)

Local Streamlit app to calculate bundle selling prices based on unit costs in an Excel sheet.

## Run locally
```bash
python -m venv .venv
# Windows
.venv\Scripts\activate
pip install -r requirements.txt
python -m streamlit run app.py
